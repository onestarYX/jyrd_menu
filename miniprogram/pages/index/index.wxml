<!--index.wxml-->

<!-- Template for menu table cell -->
<template name="menuCell">
  <view class="menuCell">
    <cover-image>
    </cover-image>

    <text>{{name}}</text>
    <text>{{price}}</text>

    <button>
      +
    </button>
  </view>
</template>

<view class="container">

  <view class="title">
    <text>欢迎来到鸡鱼肉蛋用餐</text>
  </view>

  <view>
    <scroll-view scroll-y="true">
      <!-- Here why is the getter not working? -->
      <template wx:for="{{menu}}" is="menuCell" data="{{name: item.name, price: item.price}}"></template>
    </scroll-view>
  </view>

</view>
